<% if @message.persisted? %>
  var html = '<%= j render 'messages/message', message: @message, cached: true  %>';
  var appendedMessage = document.querySelector('[data-timestamp="<%= @timestamp %>"]');
  appendedMessage.outerHTML = html;
<% end %>
