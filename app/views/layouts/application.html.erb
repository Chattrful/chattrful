<!DOCTYPE html>
<html lang="en" class="notranslate" translate="no">
  <head>
    <link rel="canonical" href="<%= request.original_url %>" />
    <title>Chattrful</title>
    <meta name="description" content="" />

    <%= meta_tags %>
    <%= favicon_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'base', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'chat_widget', 'data-turbolinks-track': 'reload' %>

  </head>

  <body id="kt_body"
        class="header-fixed header-mobile-fixed subheader-enabled subheader-fixed
          aside-enabled aside-fixed aside-minimize-hoverable page-loading
          <%= 'kt-primary--minimize aside-minimize' if @kt_aside_toggle_state == 'on' %>
          <%= params[:controller] %> <%= params[:action] %>"
  >
    <%= render 'layouts/shared/mobile_header' %>

    <div class="d-flex flex-column flex-root">
      <div class="d-flex flex-row flex-column-fluid page">
        <%= render 'layouts/shared/aside' %>

        <div class="d-flex flex-column flex-row-fluid wrapper" id="kt_wrapper">
          <%= render 'layouts/shared/header' %>

          <div class="content d-flex flex-column flex-column-fluid" id="kt_content">
            <% if content_for?(:subheader) %>
              <div class="subheader py-2 py-lg-6 subheader-solid" id="kt_subheader">
               <%= yield(:subheader) %>
              </div>
            <% end %>

            <div class="d-flex flex-column-fluid">
              <div class="<%= @container_fluid ? "container-fluid" : "container" %>">
                <%= yield %>
              </div>
            </div>
          </div>

          <%= render 'layouts/shared/footer' %>
        </div>
      </div>
    </div>
  </body>
</html>
