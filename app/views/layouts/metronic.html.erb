<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Metronic</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

    <meta name="description" content="Rails boilerplate using metronic." />
    <link rel="canonical" href="<%= request.original_url %>" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body id="kt_body"
        class="header-fixed header-mobile-fixed subheader-enabled subheader-fixed
          aside-enabled aside-fixed aside-minimize-hoverable page-loading
          <%= 'kt-primary--minimize aside-minimize' if @kt_aside_toggle_state == 'on' %>"
  >
    <%= render 'layouts/metronic/mobile_header' %>

    <div class="d-flex flex-column flex-root">
      <div class="d-flex flex-row flex-column-fluid page">
        <%= render 'layouts/metronic/aside' %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
